[project]
name = "docflow"
version = "0.1.0"
description = "Deterministic projection of structured business facts into formal enterprise documents."
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1",
    "Pillow>=10.0",
    "PyYAML>=6.0",
]

[project.scripts]
docflow = "docflow.cli:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"docflow.templates" = ["mappings/*.yaml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
