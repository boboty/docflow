__pycache__/
*.pyc
.venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Real templates and real business data must never be committed.
/templates/real/
/output/
/local/
*.local.xlsx
*.local.json

# Agent-local memory/scratch state is not product source. It also tends to
# record obsolete execution paths (old CLI invocations, template paths)
# that could mislead a future agent back onto them.
.workbuddy-ai/
