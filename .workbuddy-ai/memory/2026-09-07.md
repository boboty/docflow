# 2026-09-07

## DocFlow 批量生成（skills/docflow）

- 用 skills/docflow SKILL.md 驱动 .venv/bin/docflow CLI（不在 PATH，需用仓库内 venv 绝对路径）。
- 真实模板位于 iCloud CloudDocs/template/ 下（合同 + 送货单各一份），业务批次数据为 local/golden_batch.local.json（git-ignored，1 笔 / 18 明细行，正好等于模板容量 18 行）。
- 结果：records=1, documents_passed=2, documents_failed=0，exit 0；manifest 两条均 PASS。输出放在 /tmp 临时目录，未写入仓库。
- 教训：本环境没有设置 DOCFLOW_CONTRACT_TEMPLATE / DOCFLOW_DELIVERY_TEMPLATE 环境变量，模板路径需要按 SKILL 的顺序解析并落盘确认。
